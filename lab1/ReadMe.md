Hey quick and dirty notepad here.

so instead of ryu-manager ans_controller.py we run ryu-manager ans_router.py ans_switch.py...

This is the case since i decided to split the code in 2 classes for clear coding practices.